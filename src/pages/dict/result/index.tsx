import type { FC, ReactNode } from 'react';

interface IProps {
  children?: ReactNode;
}

const DictResultPage: FC<IProps> = () => {
  return <div>DictResultPage</div>;
};

export default DictResultPage;
