import type { FC, ReactNode } from 'react';

interface IProps {
  children?: ReactNode;
}

const AdminUserPage: FC<IProps> = () => {
  return <div>AdminUserPage</div>;
};

export default AdminUserPage;
