import type { FC, ReactNode } from 'react';

interface IProps {
  children?: ReactNode;
}

const AdminTablePage: FC<IProps> = () => {
  return <div>AdminTablePage</div>;
};

export default AdminTablePage;
